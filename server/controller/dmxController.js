const dmx = require('dmx'); // Adjust this according to the library's documentation

module.exports =
{ sendDMXValue : (channel, value) => {
 const dmx = new DMXController(/* Fill in your ENTTEC details */);

}}